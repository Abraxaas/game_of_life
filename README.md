# Игра жизни 1.1

Онлайн-версия 1.1 для геймификации рутины и полезных привычек в формате спокойной ролевой прокачки.

Пользователь создает квесты, привязывает их к жизненным статам, отмечает выполнение и получает прогресс без облака, регистрации и сервера.

## Стек

- React
- TypeScript
- Vite
- чистый CSS
- IndexedDB для хранения данных в браузере
- `localStorage` только для мелкой UI-настройки активной вкладки

## Что умеет версия 1.1

- показывает профиль и общий прогресс
- показывает отдельный блок ритма по дню, неделе и месяцу
- хранит 5 базовых статов из конфигурации
- поддерживает создание, редактирование, архивирование и удаление квестов
- поддерживает восстановление квестов из архива, если это не завершенный разовый квест
- поддерживает `daily`, `weekly`, `monthly` и `one_time` квесты
- начисляет опыт по сложности
- использует фиксированный порог опыта для каждого нового уровня
- пересчитывает уровни статов и общий прогресс профиля
- считает текущую и лучшую серию для daily-квестов
- показывает историю выполнений с быстрыми фильтрами
- поддерживает поиск и фильтрацию квестов
- позволяет отменить последнее изменение через `undo` в toast-уведомлении
- показывает короткое onboarding-окно при первом запуске и по кнопке в шапке
- умеет экспортировать и импортировать все данные в JSON
- имеет короткий seed-набор: по одному квесту на день, неделю, месяц и один разовый
- работает без backend и внешних API
- лучше адаптирована под узкие экраны

## Структура проекта

```text
src/
  app/                корневой shell и React context
  pages/              страницы главного экрана, истории и резервной копии
  components/         общие UI-компоненты
  features/
    quests/           список квестов и модалка формы
    stats/            профиль и карточки статов
    history/          экран истории
    backup/           экспорт, импорт и настройки
  shared/             константы и конфиг статов
  storage/            IndexedDB, normalizer, seed-данные
  utils/              опыт, уровни, серии, даты, валидация импорта
  types/              доменные типы
```

## Установка

Нужен Node.js 20+ и npm.

```bash
npm install
```

## Локальный запуск

```bash
npm run dev
```

По умолчанию Vite поднимает приложение на `http://localhost:5173`.

## Сборка

```bash
npm run build
```

## Деплой на GitHub Pages

Проект уже подготовлен под репозиторий `Abraxaas/game_of_life`.

- production `base` настроен в [vite.config.ts](/Users/dmitrijmalysev/Documents/prog/game/codex_repo/vite.config.ts)
- workflow деплоя лежит в [.github/workflows/deploy-pages.yml](/Users/dmitrijmalysev/Documents/prog/game/codex_repo/.github/workflows/deploy-pages.yml)

После пуша в `main` GitHub Actions сможет собрать проект и выложить его на `GitHub Pages`.

## Где хранятся данные

- основное хранилище: `IndexedDB`
- имя базы: `routine-rpg-mvp`
- вспомогательная UI-настройка активной вкладки: `localStorage`

Если `IndexedDB` недоступна, приложение показывает предупреждение и временно работает в памяти без постоянного сохранения.

## Экспорт и импорт

Экспорт:

- открой вкладку `Резервная копия и настройки`
- нажми `Экспортировать данные`
- приложение скачает JSON-файл со всем состоянием пользователя

Импорт:

- открой вкладку `Резервная копия и настройки`
- выбери JSON-файл через `Импортировать JSON`
- приложение проверит структуру данных и заменит текущее состояние в браузере

Защита:

- битый JSON не роняет приложение
- структура файла валидируется перед импортом
- поддерживается миграция snapshot-файлов версии `1 -> 2`
- импорт более новой версии схемы отклоняется с понятным сообщением

## Конфигурация статов

Базовые статы задаются в файле:

- [src/shared/statConfig.ts](/Users/dmitrijmalysev/Documents/prog/game/codex_repo/src/shared/statConfig.ts)

Именно там можно менять названия, ключи и символы статов.

## Формула уровней

- каждый новый уровень требует `100 опыта`
- формула не усложняется с ростом уровня, чтобы рутина оставалась психологически ровной

Логика лежит в:

- [src/utils/xp.ts](/Users/dmitrijmalysev/Documents/prog/game/codex_repo/src/utils/xp.ts)

## Ограничения версии 1.1

- нет облачной синхронизации
- нет аккаунтов и авторизации
- нет сервера и уведомлений
- нет полноценного offline install-режима
- нет ручной настройки статов из интерфейса
- нет автоматических напоминаний по recurring-квестам

## Что можно улучшить позже

- вынести storage-операции в более granular-репозиторий вместо сохранения полного snapshot
- добавить PWA-режим
- добавить напоминания для recurring-квестов
- добавить более глубокую аналитику по неделе и месяцу
- покрыть бизнес-логику тестами
